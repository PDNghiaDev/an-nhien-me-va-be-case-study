import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import {InfoPage} from "@/components/InfoPage";import {services} from "@/lib/case-data";export function generateStaticParams(){return services.map(({slug})=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const x=services.find(i=>i.slug===slug);return x?{title:x.title,description:x.description,alternates:{canonical:`/dich-vu/${slug}`}}:{}}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const x=services.find(i=>i.slug===slug);if(!x)notFound();return <InfoPage current={x.title} eyebrow={x.tag} title={x.title} intro={x.description}><section className="service-summary"><div><p className="eyebrow">Dành cho ai</p><h2>{x.forWho}</h2><p>{x.detail}</p></div><aside><strong>Cần xác nhận trước lịch</strong><ul><li>Người trực tiếp thực hiện</li><li>Thời lượng và chi phí</li><li>Điều kiện đổi hoặc hủy lịch</li></ul></aside></section><div className="scope-grid"><article><p className="scope-label yes">Có trong phạm vi demo</p><ul>{x.includes.map(item=><li key={item}>{item}</li>)}</ul></article><article><p className="scope-label no">Không thuộc phạm vi</p><ul>{x.notIncludes.map(item=><li key={item}>{item}</li>)}</ul></article></div><section className="timeline"><h2>Một buổi diễn ra thế nào?</h2><ol><li><span>1</span><div><h3>Xác nhận trước giờ hẹn</h3><p>Gia đình nhận lại phạm vi, người thực hiện và thời lượng.</p></div></li><li><span>2</span><div><h3>Thực hiện đúng phần đã chốt</h3><p>Mọi thay đổi đều cần được trao đổi lại.</p></div></li><li><span>3</span><div><h3>Ghi nhận sau buổi</h3><p>Nhắc lại điều đã làm và việc gia đình cần theo dõi.</p></div></li></ol></section><Link className="back-link" href="/dich-vu">← Xem tất cả dịch vụ</Link></InfoPage>}
