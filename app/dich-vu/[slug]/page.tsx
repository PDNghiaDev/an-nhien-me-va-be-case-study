import type {Metadata} from "next";import {notFound} from "next/navigation";import {InfoPage} from "@/components/InfoPage";import {services} from "@/lib/case-data";export function generateStaticParams(){return services.map(({slug})=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const x=services.find(i=>i.slug===slug);return x?{title:x.title,description:x.description,alternates:{canonical:`/dich-vu/${slug}`}}:{}}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const x=services.find(i=>i.slug===slug);if(!x)notFound();return <InfoPage eyebrow={x.tag} title={x.title} intro={x.description}><div className="grid"><article className="wide-card"><h2>Phạm vi minh họa</h2><p>{x.detail}</p><p>Nội dung cụ thể phải được chuyên gia phù hợp rà soát trước khi công bố.</p></article><article className="wide-card"><h2>Cần xác nhận trước lịch</h2><ul><li>Người trực tiếp thực hiện</li><li>Thời lượng và chi phí</li><li>Điều kiện đổi hoặc hủy lịch</li><li>Ranh giới với dịch vụ y tế</li></ul></article></div></InfoPage>}
