import type {Metadata} from "next";import Link from "next/link";import {InfoPage} from "@/components/InfoPage";import {services} from "@/lib/case-data";export const metadata:Metadata={title:"Dịch vụ",description:"Các gói chăm sóc mẹ và bé tại nhà — dữ liệu minh họa.",alternates:{canonical:"/dich-vu"}};export default function Page(){return <InfoPage eyebrow="Dịch vụ" title="Chọn theo việc gia đình đang cần." intro="Mỗi gói đều cần xác nhận lại phạm vi, thời lượng, người thực hiện và chi phí trước khi đặt."><div className="cards">{services.map(x=><article className="card" key={x.slug}><p className="tag">{x.tag}</p><h2>{x.title}</h2><p>{x.description}</p><Link href={`/dich-vu/${x.slug}`}>Xem chi tiết →</Link></article>)}</div></InfoPage>}
