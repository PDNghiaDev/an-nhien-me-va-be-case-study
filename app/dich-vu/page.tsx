import type {Metadata} from "next";import Link from "next/link";import {InfoPage} from "@/components/InfoPage";import {services} from "@/lib/case-data";export const metadata:Metadata={title:"Dịch vụ",description:"Các gói chăm sóc mẹ và bé tại nhà — dữ liệu minh họa.",alternates:{canonical:"/dich-vu"}};export default function Page(){return <InfoPage eyebrow="Dịch vụ" title="Ba nhu cầu, ba trang dịch vụ riêng." intro="So sánh nhanh ở đây, sau đó mở từng dịch vụ để xem đối tượng phù hợp, phần bao gồm và phần không bao gồm."><div className="service-list">{services.map((x,i)=><article key={x.slug}><div className="service-number">0{i+1}</div><div><p className="tag">{x.tag}</p><h2>{x.title}</h2><p>{x.description}</p><p className="small-copy"><strong>Phù hợp khi:</strong> {x.forWho}</p></div><Link className="button" href={`/dich-vu/${x.slug}`}>Xem trang dịch vụ</Link></article>)}</div><section className="decision"><h2>Chưa biết nên bắt đầu từ đâu?</h2><p>Đọc quy trình trước để biết các bước xác nhận. Không cần chọn gói khi thông tin còn thiếu.</p><Link href="/quy-trinh">Xem quy trình đặt lịch →</Link></section></InfoPage>}
