import type {MetadataRoute} from "next";import {articles,services} from "@/lib/case-data";export const dynamic="force-static";const base="https://an-nhien-me-va-be-case-study.pages.dev";export default function sitemap():MetadataRoute.Sitemap{const paths=["","/gioi-thieu","/dich-vu","/quy-trinh","/doi-ngu","/doi-ngu/ho-so-demo","/kien-thuc","/lien-he","/chinh-sach-bao-mat",...services.map(x=>`/dich-vu/${x.slug}`),...articles.map(x=>`/kien-thuc/${x.slug}`)];return paths.map(path=>({url:base+path,changeFrequency:"monthly",priority:path===""?1:.7}))}
