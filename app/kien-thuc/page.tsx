import type {Metadata} from "next";import Link from "next/link";import {InfoPage} from "@/components/InfoPage";import {articles} from "@/lib/case-data";export const metadata:Metadata={title:"Kiến thức",description:"Bài viết demo giúp gia đình chuẩn bị trước khi dùng dịch vụ.",alternates:{canonical:"/kien-thuc"}};export default function Page(){return <InfoPage eyebrow="Kiến thức" title="Đọc để hỏi đúng trước khi đặt." intro="Nội dung giáo dục không thay thế tư vấn y khoa."><div className="cards">{articles.map(x=><article className="card" key={x.slug}><h2>{x.title}</h2><p>{x.description}</p><Link href={`/kien-thuc/${x.slug}`}>Đọc bài →</Link></article>)}</div></InfoPage>}
