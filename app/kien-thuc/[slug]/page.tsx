import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import {InfoPage} from "@/components/InfoPage";import {articles} from "@/lib/case-data";export function generateStaticParams(){return articles.map(({slug})=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const x=articles.find(i=>i.slug===slug);return x?{title:x.title,description:x.description,alternates:{canonical:`/kien-thuc/${slug}`}}:{}}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const x=articles.find(i=>i.slug===slug);if(!x)notFound();return <InfoPage current={x.title} eyebrow="Hướng dẫn · 5 phút đọc" title={x.title} intro={x.description}><article className="article-body"><div className="notice">Nội dung minh họa cần được người có chuyên môn phù hợp kiểm duyệt trước khi dùng cho khách hàng thật.</div><h2>1. Ghi lại nhu cầu trước khi gọi</h2><p>Viết ngắn điều gia đình đang cần, thời gian thuận tiện và những hướng dẫn đã nhận từ cơ sở y tế. Phần này giúp cuộc trao đổi đi đúng trọng tâm.</p><h2>2. Hỏi rõ phạm vi</h2><p>Yêu cầu người tư vấn nói cụ thể việc nào được thực hiện, việc nào không và ai là người trực tiếp đến nhà.</p><blockquote>Không dùng một câu trả lời chung để thay cho phạm vi đã xác nhận bằng văn bản.</blockquote><h2>3. Chốt cách xử lý khi có thay đổi</h2><p>Ghi lại chi phí, điều kiện đổi lịch và kênh liên hệ khi cần điều chỉnh. Nếu xuất hiện dấu hiệu sức khỏe bất thường, liên hệ cơ sở y tế thay vì tiếp tục quy trình chăm sóc.</p><h2>Checklist mang theo</h2><ul><li>Nhu cầu và câu hỏi của gia đình</li><li>Tên người thực hiện và hồ sơ đã kiểm tra</li><li>Phạm vi, thời lượng và chi phí</li><li>Điểm dừng cần chuyển sang hỗ trợ y tế</li></ul></article><div className="inline-links"><Link href="/kien-thuc">← Về thư viện</Link><Link href="/dich-vu">Xem các dịch vụ →</Link></div></InfoPage>}
