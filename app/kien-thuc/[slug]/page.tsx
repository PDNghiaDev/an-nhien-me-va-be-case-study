import type {Metadata} from "next";import {notFound} from "next/navigation";import {InfoPage} from "@/components/InfoPage";import {articles} from "@/lib/case-data";export function generateStaticParams(){return articles.map(({slug})=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const x=articles.find(i=>i.slug===slug);return x?{title:x.title,description:x.description,alternates:{canonical:`/kien-thuc/${slug}`}}:{}}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const x=articles.find(i=>i.slug===slug);if(!x)notFound();return <InfoPage eyebrow="Bài viết demo" title={x.title} intro={x.description}><div className="notice">Các hướng dẫn liên quan sức khỏe cần được người có chuyên môn phù hợp kiểm duyệt.</div><h2>Ba điều cần ghi lại</h2><ol><li>Điều gia đình đang lo lắng và thông tin đã có.</li><li>Phạm vi dịch vụ đồng ý thực hiện.</li><li>Tình huống cần liên hệ cơ sở y tế.</li></ol></InfoPage>}
